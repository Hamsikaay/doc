input {
  udp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "app" => "knowledge-assistant" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}
